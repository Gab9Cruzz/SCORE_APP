# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage

# Env / secrets
.env
!.env.example

# Node / frontend
node_modules/
dist/
build/

# Docker
*.log

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# gstack (local security reports, session state — never shared)
.gstack/

# DB backups taken before running a migration script (database/09_*, etc.)
# — local safety net, never meant to be committed.
database/backups/
